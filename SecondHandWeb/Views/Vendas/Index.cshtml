@model IEnumerable<Entidades.Modelos.Venda>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DiasAposPost)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.isSold)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DiasAposPost)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.isSold)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.VendaID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.VendaID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.VendaID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
